Minimal NIFTY Vercel project. Root package.json added to build frontend and copy frontend/dist -> ./dist for Vercel.
Run `npm run build` at repo root to verify ./dist is created.
"# -NIFTY-Vercel-Node-React-V2" 
